class Player{
      /*ellipseMode(RADIUS);
  ellipse(a.position.x, a.position.y, 20,20);
  */

    /*var options = {
    isStatic : true
  }
  a = Bodies.circle(200,400,30,options);
  World.add(world, a);*/

  //var a;
}